# CEDAR

**C**apped, **E**psilon-bounded, **D**ynamic, **A**rray-based, cache-**R**esident

A dynamic, cache resident learned index that holds an exact epsilon guarantee under
inserts and deletes with bounded, noncascading tail latency.